schema {
  query: Query
}

type Query {
  getNotifications(userID: String!): [Notification!]!
}

type Notification {
  id: ID!
  userID: String!
  postID: String!
  postAuthorID: String!
  content: String!
  read: Boolean!
  createdAt: String!
} 